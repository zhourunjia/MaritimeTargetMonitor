<Application x:Class="Maritime.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Maritime.App">
    <Application.Resources>
        <!-- Theme colors -->
        <Color x:Key="PrimaryColor">#2F6FED</Color>
        <Color x:Key="PrimaryDarkColor">#1E4BB8</Color>
        <Color x:Key="AccentColor">#0EA5E9</Color>
        <Color x:Key="BackgroundColor">#F5F7FA</Color>
        <Color x:Key="SurfaceColor">#FFFFFF</Color>
        <Color x:Key="SurfaceAltColor">#EEF2F7</Color>
        <Color x:Key="BorderColor">#D9E1EC</Color>
        <Color x:Key="TextColor">#1F2937</Color>
        <Color x:Key="TextMutedColor">#6B7280</Color>
        <Color x:Key="MenuPanelColor">#0B1322</Color>
        <Color x:Key="MenuPanelBorderColor">#243246</Color>
        <Color x:Key="MenuTextColor">#FFFFFF</Color>
        <Color x:Key="MenuMutedColor">#D7E4F6</Color>
        <Color x:Key="MenuAccentColor">#36C3FF</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="{StaticResource SurfaceAltColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>
        <SolidColorBrush x:Key="MenuPanelBrush" Color="{StaticResource MenuPanelColor}"/>
        <SolidColorBrush x:Key="MenuPanelBorderBrush" Color="{StaticResource MenuPanelBorderColor}"/>
        <SolidColorBrush x:Key="MenuTextBrush" Color="{StaticResource MenuTextColor}"/>
        <SolidColorBrush x:Key="MenuMutedBrush" Color="{StaticResource MenuMutedColor}"/>
        <SolidColorBrush x:Key="MenuAccentBrush" Color="{StaticResource MenuAccentColor}"/>

        <LinearGradientBrush x:Key="MenuActiveBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#1B2D47" Offset="0"/>
            <GradientStop Color="#132238" Offset="1"/>
        </LinearGradientBrush>

        <!-- Base typography -->
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3EAF6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#D6E1F5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <!-- Menu styles -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource MenuTextBrush}"/>
            <Setter Property="Background" Value="#13233A"/>
            <Setter Property="BorderBrush" Value="#27415F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,10,10,10"/>
            <Setter Property="Margin" Value="12,8,12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Grid>
                                <Border x:Name="AccentBar" Width="4" Background="Transparent" CornerRadius="10,0,0,10" HorizontalAlignment="Left"/>
                                <TextBlock Text="{TemplateBinding Content}"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontSize="{TemplateBinding FontSize}"
                                           FontWeight="{TemplateBinding FontWeight}"
                                           Margin="{TemplateBinding Padding}"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1A2F4B"/>
                                <Setter TargetName="AccentBar" Property="Background" Value="#2FD3FF"/>
                                <Setter Property="BorderBrush" Value="#3A5D82"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#11223A"/>
                            </Trigger>
                            <Trigger Property="local:MenuButtonHelper.IsActive" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource MenuActiveBrush}"/>
                                <Setter TargetName="AccentBar" Property="Background" Value="{StaticResource MenuAccentBrush}"/>
                                <Setter Property="BorderBrush" Value="#2F5A86"/>
                                <Setter Property="Foreground" Value="{StaticResource MenuTextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuSubButtonStyle" TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="28,8,10,8"/>
            <Setter Property="Margin" Value="20,6,12,0"/>
        </Style>

        <Style x:Key="MenuExpanderStyle" TargetType="Expander">
            <Setter Property="Foreground" Value="{StaticResource MenuTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource MenuPanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource MenuPanelBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <StackPanel>
                                <ToggleButton x:Name="HeaderSite"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Foreground="{TemplateBinding Foreground}"
                                              Padding="{TemplateBinding Padding}"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                    <DockPanel LastChildFill="True">
                                        <Path x:Name="Arrow"
                                              Data="M 0 0 L 6 6 L 12 0"
                                              Stroke="{TemplateBinding Foreground}"
                                              StrokeThickness="2"
                                              Width="12"
                                              Height="7"
                                              Margin="0,0,8,0"
                                              RenderTransformOrigin="0.5,0.5">
                                            <Path.RenderTransform>
                                                <RotateTransform Angle="0"/>
                                            </Path.RenderTransform>
                                        </Path>
                                        <TextBlock Text="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}"
                                                   Foreground="{TemplateBinding Foreground}"
                                                   FontSize="{TemplateBinding FontSize}"
                                                   FontWeight="{TemplateBinding FontWeight}"
                                                   VerticalAlignment="Center"/>
                                    </DockPanel>
                                </ToggleButton>
                                <ContentPresenter x:Name="ExpandSite" Visibility="Collapsed" Margin="0,4,0,8"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Arrow" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="180"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger SourceName="HeaderSite" Property="IsMouseOver" Value="True">
                                <Setter TargetName="HeaderSite" Property="Background" Value="#142338"/>
                            </Trigger>
                            <Trigger SourceName="HeaderSite" Property="IsPressed" Value="True">
                                <Setter TargetName="HeaderSite" Property="Background" Value="#0B1424"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
