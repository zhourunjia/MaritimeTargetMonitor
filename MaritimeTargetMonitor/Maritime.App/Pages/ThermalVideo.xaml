<Page x:Class="Maritime.App.Pages.ThermalVideo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Maritime.App.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="ThermalVideo">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Border Background="#0F1115" Margin="16" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                    <TextBlock Text="热成像视频" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text=" | " Foreground="#666666" FontSize="12" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBlock Text="等待信号" Foreground="#00ff00" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock Text=" | " Foreground="#666666" FontSize="12" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBlock Text="FPS: --" Foreground="#00ff00" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="信号源：127.0.0.1:5001" Foreground="#00ff00" FontSize="12" Margin="10,0,10,10" VerticalAlignment="Top"/>
                <TextBlock Grid.Row="2" Text="暂无视频信号" Foreground="#ffffff" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Page>
